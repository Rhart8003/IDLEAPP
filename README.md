# IDLE

**Put your stuff to work.**

Private-beta web deployment for IDLE. The hosted beta intentionally runs with demo intelligence and no real payment processing while the production providers are being enabled.

## Run

```bash
npm install
npm start
```

Health endpoint: `/api/health`

## Deploy

The repository includes a `render.yaml` blueprint for the free Render web-service tier.
